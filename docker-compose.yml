services:
  mysql:
    image: mysql:8.4.0
    container_name: nexo-mysql
    environment:
      MYSQL_DATABASE: nexo
      MYSQL_ROOT_PASSWORD: ITB@123456
    ports:
      - "3306:3306"
    restart: always