services:
  mysql:
    image: mysql:latest
    container_name: mysql
    environment:
      MYSQL_DATABASE: eventclean
      MYSQL_ROOT_PASSWORD: ITB@123456
    ports:
      - "3306:3306"
    restart: always